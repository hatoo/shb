[package]
name = "shb"
version = "0.4.1"
authors = ["hatoo <hato2000@gmail.com>"]
edition = "2024"
rust-version = "1.91"
description = "Simple HTTP Benchmarker: an io_uring based HTTP/1.1, HTTP/2 and HTTP/3 load generator"
documentation = "https://github.com/hatoo/shb"
repository = "https://github.com/hatoo/shb"
readme = "README.md"
license = "MIT"
keywords = ["cli", "load-testing", "benchmark", "http", "io-uring"]
categories = [
    "command-line-utilities",
    "network-programming",
    "web-programming::http-client",
    "development-tools::profiling",
]

[dependencies]
anyhow = "1.0.104"
clap = { version = "4.6.6", features = ["derive"] }
humantime = "2.4.0"
io-uring = "0.7.14"
libc = "0.2"
memchr = "2.8.3"
mimalloc = { version = "0.1.52", default-features = false, optional = true }
num_cpus = "1.17.0"
# Already linked through rustls; used directly only for the fixed AES-GCM that
# authenticates a Retry packet (RFC 9001 Section 5.8)
ring = "0.17"
rustls = { version = "0.23.43", default-features = false, features = ["ring", "std", "tls12"] }
serde_json = "1.0.151"
socket2 = "0.6.5"

[dev-dependencies]
axum = { version = "0.8", features = ["http2"] }
bytes = "1.12.1"
h3 = "0.0.8"
h3-quinn = "0.0.10"
http = "1.5.0"
quinn = { version = "0.11", default-features = false, features = ["runtime-tokio", "rustls", "ring"] }
rcgen = "0.14.9"
tokio = { version = "1", features = ["rt-multi-thread", "net"] }

# Optimised build used for released binaries. Kept separate from `release`
# so local `cargo build --release` stays fast.
[profile.dist]
inherits = "release"
lto = true
codegen-units = 1
strip = true

[features]
# On by default because it is what makes a musl build worth having: musl's own
# allocator doubles shb's userspace CPU per request, which costs 36% of HTTP/3
# throughput. --no-default-features falls back to whatever the platform gives.
default = ["mimalloc"]
mimalloc = ["dep:mimalloc"]
